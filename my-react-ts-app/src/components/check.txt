const handleSaveClick = async (id?: string | number) => 
{ try { 
    
    if (id !== undefined) { // Update existing education entry//}
     const editedEducation = educations.find((edu) => edu.id === id); 
     const response = await axios.put(`http://localhost:3001/api/educations/${id}`, 
        { university: editedEducation?.university, 
    degree: editedEducation?.degree, 
    graduationYear: editedEducation?.graduationYear, });
 const updatedEducation = response.data; 
 setEducations((prevEducations) => prevEducations.map((edu) => edu.id === id ? 
 { ...edu, ...updatedEducation, isEditing: false } : edu ) ); } 
 else { // Add new education entry 
    const response = await axios.post('http://localhost:3001/api/educations', 
    { university: newEducation.university,
         degree: newEducation.degree,
          graduationYear: newEducation.graduationYear, });
    const createdEducation = response.data; setEducations((prevEducations) => [...prevEducations, createdEducation]);
     setIsAdding(false); 
     setNewEducationSearchTerm('');
     } } catch (error) { console.error('Error saving education entry:', error); } };